import type { SortStep } from "../types/sorting";

export function bubbleSort(array: number[]): SortStep[] {
  const steps: SortStep[] = [];

  // Implementation coming next milestone

  return steps;
}